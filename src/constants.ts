/**
 * Maximum "server" response delay.
 */
export const STORAGE_MAX_DELAY = Number(import.meta.env.VITE_APP_STORAGE_MAX_DELAY);

/**
 * Internal "server" error probability.
 */
export const STORAGE_RANDOM_FAIL = Number(import.meta.env.VITE_APP_STORAGE_RANDOM_FAIL);
